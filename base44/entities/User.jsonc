{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "plan": {
      "type": "string",
      "description": "Subscription tier unlocked via Base44 Payments",
      "enum": [
        "free",
        "pro",
        "elite"
      ],
      "default": "free"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}